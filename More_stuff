E1.
def hasword(word)
  if word =~ /lab/i
  puts word
else 
  puts "No mas!"
end
end

hasword("laboratory")
hasword("experiment")
hasword("Pans Labyrinth")
hasword("elaborate")
hasword("polar bear")

E2.
Due to the .call not activated, nothing is printed to the screen.

E3.
Exception handling is a specific process that deals with errors in a manageable and predictable way.  
It is a way of handling the error by changing the flow of control without leaving the entire program.

E4.
def execute(&block)
  block.call
end

execute { puts "Hello from inside the execute method!" }

E5
The method parameter block is missing the ampersand sign & that allows a block to be passed as a parameter.
